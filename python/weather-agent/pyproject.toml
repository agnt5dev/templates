[project]
name = "weather_agent"
version = "0.1.10"
description = "Weather Agent built using AGNT5 SDK"
readme = "README.md"

requires-python = ">=3.11"
dependencies = [
    "agnt5~=0.13.6",
    "httpx>=0.28.1",
    "pydantic>=2.12.5",
]

[project.scripts]
weather_agent = "weather_agent:main"

[build-system]
requires = ["uv-build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
